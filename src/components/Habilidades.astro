---
import frontend from '@/json/frontend.json'
import backend from '@/json/backend.json'
import herramientas from '@/json/herramientas.json'
---

<section>
  <article>
    <h2>Mis Habilidades técnicas</h2>
    <p>Tecnologías y habilidades que domino.</p>
  </article>
  <article>
    <div>
      <h3>Frontend</h3>
      <ul>
        {frontend.map(({herramienta}) => <li>{herramienta}</li>)}
      </ul>
    </div>
    <div>
      <h3>Backend</h3>
      <ul>
        {backend.map(({herramienta}) => <li>{herramienta}</li>)}
      </ul>
    </div>
    <div>
      <h3>Herramientas</h3>
      <ul>
        {herramientas.map(({herramienta}) => (
          <li>{herramienta}</li>
          ))}
      </ul>
    </div>
  </article>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
  article:last-child {
    display: grid;
    gap: 1.5rem;
    margin: 0 auto;
    width: 100%;
    max-width: 64rem;
    padding-top: 3rem;
    padding-bottom: 3rem;
  }
  article > div {
    position: relative;
    height: 350px;
    padding: 15px;
    background-color: rgb(17, 24, 39);
    border-radius: 0.75rem;
    color: white;
  }

  ul {
    list-style: none;
    & > li {
      margin-left: 3px;
    }
  }

  @media screen and (min-width: 1024px) {
    article:last-child {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 3rem;
  }
  }
</style>